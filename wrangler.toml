name = "wiseadvisor"
compatibility_date = "2024-02-11"
pages_build_output_dir = "dist"

[[d1_databases]]
binding = "DB"
database_name = "wiseadvisor-db"
database_id = "to-be-replaced-by-user" # User needs to run `npx wrangler d1 create wiseadvisor-db` and paste the ID here

[vars]
# RESEND_API_KEY = "re_..." # Set in Cloudflare Dashboard for production
